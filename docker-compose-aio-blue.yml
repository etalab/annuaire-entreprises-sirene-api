version: '3'
services:
  http-proxy:
    build:
      context: ./aio
    environment:
       - CURRENT_COLOR=blue
       - NEXT_COLOR=green
    networks:
       - traefik
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.aio.rule=Host(`localhost`)"
      - "traefik.http.routers.aio.entrypoints=aio"
      - "traefik.port=4400"
    container_name: aio-blue
networks:
    traefik:
        name: only-search_backendsiren
